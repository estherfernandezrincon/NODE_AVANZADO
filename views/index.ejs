<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>
    <%= title %>
  </h1>
  <p><%= __('¿Qué quieres: comprar o vender?') %></p>


  </p>
  <% anuncios.forEach(element=> { %>
    <div class="grid-container">
      <div class="item">
        <%= element.nombre %>
          <ul class="contenido">
            <li>
              <%= element.venta %>
            </li>
            <li>
              <%= element.precio %>
            </li>
            <ul>
              tags:
              <li>
                <%= element.tags[0] %>
              </li>
              <li>
                <%= element.tags[1] %>
              </li>

            </ul>
      </div>

    </div>

    <% }) %>

</body>

</html>